import { describe, expect, it } from 'vitest';
import { scoringService } from '../../src/services/scoringService.js';
describe('scoringService', () => { it('scores richer listings higher', () => { const low = scoringService.score({platform:'generic',sourceUrl:'https://x.test'}, []); const high = scoringService.score({platform:'generic',sourceUrl:'https://x.test',year:2020,make:'Toyota',model:'Camry',trim:'SE',vin:'1HGCM82633A004352',mileage:40000,location:'NY'}, Array.from({length:6},()=>({platform:'generic',sourceUrl:'https://c.test',priceUsd:20000}))); expect(high).toBeGreaterThan(low); expect(high).toBeLessThanOrEqual(1); }); });
