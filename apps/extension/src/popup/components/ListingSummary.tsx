import { Car, MapPin, Gauge, DollarSign } from 'lucide-react'; import type { ListingInput } from '../../shared/types';
export default function ListingSummary({ listing }: { listing: ListingInput }) { return <div className="card"><h2><Car size={18}/> Detected listing</h2><strong>{listing.title ?? [listing.year, listing.make, listing.model, listing.trim].filter(Boolean).join(' ')}</strong><p className="muted">{listing.platform} · {listing.sourceUrl}</p><p><DollarSign size={14}/> {listing.priceUsd ? `$${listing.priceUsd.toLocaleString()}` : 'Price unknown'} · <Gauge size={14}/> {listing.mileage ? `${listing.mileage.toLocaleString()} mi` : 'Mileage unknown'} · <MapPin size={14}/> {listing.location ?? 'Location unknown'}</p></div>; }
