import type { ComparableListing } from '../../shared/types';
export default function ComparableTable({ comps }: { comps: ComparableListing[] }) { if (!comps.length) return <p className="muted">No comparables found yet. Estimate uses listing price fallback.</p>; return <table><thead><tr><th>Year</th><th>Make</th><th>Model</th><th>Trim</th><th>Mileage</th><th>Price</th><th>Source</th><th>Distance</th></tr></thead><tbody>{comps.map((c,i)=><tr key={i}><td>{c.year}</td><td>{c.make}</td><td>{c.model}</td><td>{c.trim}</td><td>{c.mileage?.toLocaleString()}</td><td>{c.priceUsd ? `$${c.priceUsd.toLocaleString()}` : ''}</td><td>{c.platform}</td><td>{c.distanceMiles ? `${c.distanceMiles} mi` : ''}</td></tr>)}</tbody></table>; }
