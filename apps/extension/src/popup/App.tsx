import { useEffect, useState } from 'react'; import { AlertCircle, DollarSign, RefreshCw, TrendingUp } from 'lucide-react';
import type { ExtensionMessage } from '../shared/messages'; import type { ListingInput, ValuationResponse } from '../shared/types';
import ListingSummary from './components/ListingSummary'; import ConfidenceMeter from './components/ConfidenceMeter'; import ComparableTable from './components/ComparableTable'; import FairValueBadge from './components/FairValueBadge';
function send<T extends ExtensionMessage>(message: ExtensionMessage) { return chrome.runtime.sendMessage(message) as Promise<T>; }
export default function App() { const [listing,setListing]=useState<ListingInput|null>(null); const [result,setResult]=useState<ValuationResponse|null>(null); const [loading,setLoading]=useState(false); const [error,setError]=useState<string|null>(null);
 const extract=async()=>{setLoading(true);setError(null);setResult(null);try{const res=await send<ExtensionMessage>({type:'EXTRACT_LISTING'}); if(res.type==='LISTING_EXTRACTED') setListing(res.listing); else if(res.type==='VALUATION_ERROR') setError(res.error); }catch(e){setError(e instanceof Error?e.message:'Could not extract listing')} finally{setLoading(false)}};
 useEffect(()=>{void extract()},[]); const estimate=async()=>{if(!listing)return;setLoading(true);setError(null);try{const res=await send<ExtensionMessage>({type:'REQUEST_VALUATION',listing}); if(res.type==='VALUATION_RESULT') setResult(res.result); else if(res.type==='VALUATION_ERROR') setError(res.error);}catch(e){setError(e instanceof Error?e.message:'Valuation failed')}finally{setLoading(false)}};
 return <main style={{padding:16,display:'grid',gap:12}}><header style={{display:'flex',alignItems:'center',justifyContent:'space-between'}}><h1><TrendingUp size={20}/> Fair Value</h1><button className="btn" onClick={extract} disabled={loading}><RefreshCw size={14}/> Refresh</button></header>{error&&<div className="card" style={{color:'#991b1b'}}><AlertCircle size={16}/> {error}</div>}{listing ? <ListingSummary listing={listing}/> : <div className="card muted">{loading?'Scanning page…':'No listing detected'}</div>}<button className="btn" onClick={estimate} disabled={!listing||loading}><DollarSign size={16}/> Estimate Fair Value</button>{result&&<section className="card"><div style={{display:'flex',justifyContent:'space-between',alignItems:'center'}}><h2>${result.valuation.fairValueLow.toLocaleString()}–${result.valuation.fairValueHigh.toLocaleString()}</h2><FairValueBadge pos={result.marketPosition}/></div><p>Median estimate: <b>${result.valuation.fairValueMedian.toLocaleString()}</b> from {result.valuation.comparableCount} comparables.</p><ConfidenceMeter score={result.valuation.confidenceScore}/><h3>Comparables</h3><ComparableTable comps={result.valuation.comparables}/></section>}</main>; }
